<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.xml</groupId>
        <artifactId>xiaobin-job</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>

    <artifactId>xiaobin-job-admin</artifactId>
    <packaging>jar</packaging>
    <description>XXL-Job Admin 调度中心 - 定时任务管理Web UI</description>

    <dependencies>
        <!-- Job核心模块 -->
        <dependency>
            <groupId>com.xml</groupId>
            <artifactId>xiaobin-job-core</artifactId>
        </dependency>

        <!-- XXL-Job Admin (调度中心代码 + Freemarker模板 + 静态资源) -->
<!--        <dependency>-->
<!--            <groupId>com.xuxueli</groupId>-->
<!--            <artifactId>xxl-job-admin</artifactId>-->
<!--        </dependency>-->

        <!-- Freemarker (Admin UI模板引擎) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-freemarker</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

</project>
